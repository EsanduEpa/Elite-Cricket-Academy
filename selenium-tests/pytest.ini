[pytest]
addopts = -ra
testpaths = tests
python_files = test_*.py
markers =
    smoke: fast checks for critical access and authentication paths
    regression: browser regression coverage for existing user workflows
